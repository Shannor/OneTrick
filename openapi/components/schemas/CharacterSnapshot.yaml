x-oapi-codegen-extra-tags:
  firestore: characterSnapshot
type: object
required:
  - loadout
  - createdAt
  - characterId
  - id
  - userId
  - hash
  - name
  - updatedAt
properties:
  id:
    type: string
    x-go-name: ID
    description: Id of the snapshot
    x-oapi-codegen-extra-tags:
      firestore: id
  name:
    type: string
    x-go-name: name
    description: >-
      Name of the snapshot, will probably be generated by default by the system
      but can be changed by a user
    x-oapi-codegen-extra-tags:
      firestore: name
  description:
    type: string
    description: >-
      Description of the snapshot. Will be empty by default and added by the user later.
    x-oapi-codegen-extra-tags:
      firestore: description
  userId:
    type: string
    x-go-name: userID
    description: Id of the user it belongs to
    x-oapi-codegen-extra-tags:
      firestore: userId
  hash:
    type: string
    description: Hash of all the items to give us a unique key
    x-oapi-codegen-extra-tags:
      firestore: hash
  characterId:
    type: string
    x-go-name: characterID
    description: Id of the character being recorded
    x-oapi-codegen-extra-tags:
      firestore: characterId
  stats:
    type: object
    x-oapi-codegen-extra-tags:
      firestore: stats
    additionalProperties:
      $ref: ./ClassStat.yaml
  createdAt:
    type: string
    format: date-time
    description: Timestamp for when the snapshot was first created
    x-oapi-codegen-extra-tags:
      firestore: createdAt
  updatedAt:
    type: string
    format: date-time
    description: >-
      Timestamp for when the snapshot was last updated or when a history entry
      was made for it.
    x-oapi-codegen-extra-tags:
      firestore: updatedAt
  loadout:
    $ref: ./Loadout.yaml
