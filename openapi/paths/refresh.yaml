post:
  operationId: RefreshToken
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - code
          properties:
            code:
              type: string
  responses:
    '200':
      description: Refreshing token
      content:
        application/json:
          schema:
            $ref: ../components/schemas/AuthResponse.yaml
